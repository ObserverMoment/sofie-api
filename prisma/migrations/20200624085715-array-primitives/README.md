# Migration `20200624085715-array-primitives`

This migration has been generated by ObserverMoment at 6/24/2020, 8:57:15 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Challenge" ALTER COLUMN "scope" SET DEFAULT E'PRIVATE';

ALTER TABLE "public"."Group" ALTER COLUMN "scope" SET DEFAULT E'PRIVATE';

ALTER TABLE "public"."Move" ADD COLUMN "validRepTypes" "WorkoutMoveRepType"[]  ,
ALTER COLUMN "scope" SET DEFAULT E'PRIVATE';

ALTER TABLE "public"."User" ALTER COLUMN "subscriptionLevel" SET DEFAULT E'FREE',
ALTER COLUMN "unitSystem" SET DEFAULT E'METRIC';

ALTER TABLE "public"."Workout" ALTER COLUMN "difficultyLevel" SET DEFAULT E'ONE',
ALTER COLUMN "scope" SET DEFAULT E'PRIVATE',
ALTER COLUMN "workoutScoreType" SET DEFAULT E'AMREPS';

ALTER TABLE "public"."WorkoutMove" ALTER COLUMN "distanceUnit" SET DEFAULT E'METRES',
ALTER COLUMN "repType" SET DEFAULT E'REPS';

ALTER TABLE "public"."WorkoutSection" DROP COLUMN "pyramidStructure",
ADD COLUMN "pyramidStructure" integer []  ;

ALTER TABLE "public"."WorldRecord" ALTER COLUMN "workoutScoreType" SET DEFAULT E'AMREPS';
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200617135056-workout-demo-video-url..20200624085715-array-primitives
--- datamodel.dml
+++ datamodel.dml
@@ -6,9 +6,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 model Benchmark {
   id               String          @default(uuid()) @id
@@ -115,18 +115,19 @@
   WorkoutMove        WorkoutMove[]
 }
 model Move {
-  id                   String          @default(uuid()) @id
-  createdAt            DateTime        @default(now())
-  createdBy            User?           @relation(fields: [createdById], references: [id])
+  id                   String               @default(uuid()) @id
+  createdAt            DateTime             @default(now())
+  createdBy            User?                @relation(fields: [createdById], references: [id])
   createdById          String?
-  scope                AccessScopeType @default(PRIVATE)
-  name                 String          @unique
+  scope                AccessScopeType      @default(PRIVATE)
+  name                 String               @unique
   description          String?
   demoVideoUrl         String?
-  requiredEquipments   Equipment[]     @relation("moveToRequiredEquipments", references: [id])
-  selectableEquipments Equipment[]     @relation("moveToSelectableEquipments", references: [id])
+  validRepTypes        WorkoutMoveRepType[]
+  requiredEquipments   Equipment[]          @relation("moveToRequiredEquipments", references: [id])
+  selectableEquipments Equipment[]          @relation("moveToSelectableEquipments", references: [id])
   workoutMoves         WorkoutMove[]
 }
 model WorkoutMove {
@@ -151,9 +152,9 @@
   createdAt        DateTime      @default(now())
   sortPosition     Int
   name             String?
   isPyramid        Boolean       @default(false)
-  pyramidStructure String?       @default("15,12,9")
+  pyramidStructure Int[]
   isTabata         Boolean       @default(false)
   timecap          Int?
   rounds           Int           @default(1)
   workout          Workout       @relation(fields: [workoutId], references: [id])
@@ -310,10 +311,9 @@
 // AMREPS in reps
 // TIME in seconds
 // LOAD in kgs
-// EMON in rounds completed (if doing standard EMON then this unit value is minutes)
-// Also used for: Every 5 miniutes on the 5 minutes etc - free text in description?
+// EMON in reps
 enum WorkoutScoreType {
   AMREPS
   EMOM
   FORTIME
```



# Migration `20200527112432-create-move-variation`

This migration has been generated by ObserverMoment at 5/27/2020, 11:24:32 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."MoveVariation" (
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"createdById" text   ,"description" text   ,"id" text  NOT NULL ,"moveId" text  NOT NULL ,"name" text  NOT NULL ,
    PRIMARY KEY ("id"))

ALTER TABLE "public"."Challenge" ALTER COLUMN "scope" SET DEFAULT E'PRIVATE';

ALTER TABLE "public"."Equipment" ADD COLUMN "moveVariationId" text   ;

ALTER TABLE "public"."Group" ALTER COLUMN "scope" SET DEFAULT E'PRIVATE';

ALTER TABLE "public"."Move" ALTER COLUMN "scope" SET DEFAULT E'PRIVATE';

ALTER TABLE "public"."User" ALTER COLUMN "subscriptionLevel" SET DEFAULT E'FREE',
ALTER COLUMN "unitSystem" SET DEFAULT E'METRIC';

ALTER TABLE "public"."Workout" ALTER COLUMN "difficultyLevel" SET DEFAULT E'ONE',
ALTER COLUMN "scope" SET DEFAULT E'PRIVATE',
ALTER COLUMN "workoutScoreType" SET DEFAULT E'AMREPS';

ALTER TABLE "public"."WorkoutMove" ALTER COLUMN "repType" SET DEFAULT E'REPS';

ALTER TABLE "public"."WorldRecord" ALTER COLUMN "workoutScoreType" SET DEFAULT E'AMREPS';

ALTER TABLE "public"."MoveVariation" ADD FOREIGN KEY ("createdById")REFERENCES "public"."User"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."MoveVariation" ADD FOREIGN KEY ("moveId")REFERENCES "public"."Move"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Equipment" ADD FOREIGN KEY ("moveVariationId")REFERENCES "public"."MoveVariation"("id") ON DELETE SET NULL  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200526163831-move-available-equipments..20200527112432-create-move-variation
--- datamodel.dml
+++ datamodel.dml
@@ -6,9 +6,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 model Benchmark {
   id               String          @default(uuid()) @id
@@ -119,8 +119,21 @@
   availableEquipments Equipment[]     @relation(references: [id])
   workoutMoves        WorkoutMove[]
 }
+model MoveVariation {
+  id          String      @default(uuid()) @id
+  createdAt   DateTime    @default(now())
+  createdBy   User?       @relation(fields: [createdById], references: [id])
+  createdById String?
+  name        String
+  description String?
+  move        Move        @relation(fields: [moveId], references: [id])
+  moveId      String
+  equipments  Equipment[]
+
+}
+
 model WorkoutMove {
   id                  String             @default(uuid()) @id
   createdAt           DateTime           @default(now())
   sortPosition        Int
@@ -320,5 +333,5 @@
 enum UnitSystem {
   IMPERIAL
   METRIC
-}
+}
```



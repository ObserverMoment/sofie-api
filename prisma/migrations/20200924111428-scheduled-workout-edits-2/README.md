# Migration `20200924111428-scheduled-workout-edits-2`

This migration has been generated by ObserverMoment at 9/24/2020, 12:14:28 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."ScheduledWorkout" DROP COLUMN "completed",
ADD COLUMN "completedOn" timestamp(3)   
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200924104950-scheduled-workout-edits..20200924111428-scheduled-workout-edits-2
--- datamodel.dml
+++ datamodel.dml
@@ -6,9 +6,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 // model ScoreSubmission {
 //   id                     String                @default(uuid()) @id
@@ -137,17 +137,8 @@
   Workout       Workout[]
   LoggedWorkout LoggedWorkout[]
 }
-// model WorkoutTag {
-//   id          String    @default(uuid()) @id
-//   createdAt   DateTime  @default(now())
-//   createdBy   User?     @relation(fields: [createdById], references: [id])
-//   createdById String?
-//   name        String
-//   workouts    Workout[]
-// }
-
 model GymProfile {
   id                String             @default(uuid()) @id
   user              User               @relation(fields: [userId], references: [id])
   userId            String
@@ -191,29 +182,17 @@
   user            User           @relation(fields: [userId], references: [id])
   userId          String
   notes           String?
   scheduledAt     DateTime       @default(now())
-  completed       Boolean        @default(false)
+  completedOn     DateTime?
   workout         Workout?       @relation(fields: [workoutId], references: [id])
   workoutId       String?
   loggedWorkout   LoggedWorkout? @relation(fields: [loggedWorkoutId], references: [id])
   loggedWorkoutId String?
   gymProfile      GymProfile?    @relation(fields: [gymProfileId], references: [id])
   gymProfileId    String?
 }
-// Users can apply these to any workout they want, whereas WorkoutTags can only
-// be applied to workouts that you create / own.
-// Used for filtering, categorising your favourite workouts.
-// model LikedWorkoutTag {
-//   id            String         @default(uuid()) @id
-//   createdAt     DateTime       @default(now())
-//   createdBy     User?          @relation(fields: [createdById], references: [id])
-//   createdById   String?
-//   name          String
-//   likedWorkouts LikedWorkout[]
-// }
-
 model LikedWorkout {
   createdAt DateTime @default(now())
   user      User     @relation(fields: [userId], references: [id])
   userId    String
@@ -349,11 +328,9 @@
   moves             Move[]
   workouts          Workout[]
   scheduledWorkouts ScheduledWorkout[]
   loggedWorkouts    LoggedWorkout[]
-  // workoutTags                        WorkoutTag[]
   likedWorkouts     LikedWorkout[]
-  // LikedWorkoutTag                    LikedWorkoutTag[]
   gymProfiles       GymProfile[]
 }
 // Enums
```



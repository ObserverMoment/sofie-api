# Migration `20200728165202-workout-section-pyramid-types`

This migration has been generated by ObserverMoment at 7/28/2020, 4:52:02 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."WorkoutSection" DROP COLUMN "isPyramid",
DROP COLUMN "isTabata",
DROP COLUMN "pyramidStructure",
ADD COLUMN "repPyramid" boolean  NOT NULL DEFAULT false,
ADD COLUMN "repPyramidStructure" integer []  ,
ADD COLUMN "weightPyramid" boolean  NOT NULL DEFAULT false,
ADD COLUMN "weightPyramidStructure" integer []  ;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200724120724-workout-move-load-unit-2..20200728165202-workout-section-pyramid-types
--- datamodel.dml
+++ datamodel.dml
@@ -6,9 +6,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model ScoreSubmission {
   id                     String                @default(uuid()) @id
@@ -151,20 +151,21 @@
   workoutSectionId    String
 }
 model WorkoutSection {
-  id               String        @default(uuid()) @id
-  createdAt        DateTime      @default(now())
-  sortPosition     Int
-  name             String?
-  isPyramid        Boolean       @default(false)
-  pyramidStructure Int[]
-  isTabata         Boolean       @default(false)
-  timecap          Int?
-  rounds           Int           @default(1)
-  workout          Workout       @relation(fields: [workoutId], references: [id])
-  workoutId        String
-  workoutMoves     WorkoutMove[]
+  id                     String        @default(uuid()) @id
+  createdAt              DateTime      @default(now())
+  sortPosition           Int
+  name                   String?
+  repPyramid             Boolean       @default(false)
+  repPyramidStructure    Int[]
+  weightPyramid          Boolean       @default(false)
+  weightPyramidStructure Int[]
+  timecap                Int?
+  rounds                 Int           @default(1)
+  workout                Workout       @relation(fields: [workoutId], references: [id])
+  workoutId              String
+  workoutMoves           WorkoutMove[]
 }
 model Workout {
   id                String             @default(uuid()) @id
```



# Migration `20200616164228-workout-move-remove-gendering`

This migration has been generated by ObserverMoment at 6/16/2020, 4:42:28 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TYPE "DistanceUnit" AS ENUM ('METRES', 'KILOMETRES', 'YARDS', 'MILES');

ALTER TABLE "public"."Challenge" ALTER COLUMN "scope" SET DEFAULT E'PRIVATE';

ALTER TABLE "public"."Group" ALTER COLUMN "scope" SET DEFAULT E'PRIVATE';

ALTER TABLE "public"."Move" ALTER COLUMN "scope" SET DEFAULT E'PRIVATE';

ALTER TABLE "public"."User" ALTER COLUMN "subscriptionLevel" SET DEFAULT E'FREE',
ALTER COLUMN "unitSystem" SET DEFAULT E'METRIC';

ALTER TABLE "public"."Workout" DROP COLUMN "genderEquality",
ALTER COLUMN "difficultyLevel" SET DEFAULT E'ONE',
ALTER COLUMN "scope" SET DEFAULT E'PRIVATE',
ALTER COLUMN "workoutScoreType" SET DEFAULT E'AMREPS';

ALTER TABLE "public"."WorkoutMove" DROP COLUMN "femaleLoadAmountKgs",
DROP COLUMN "femaleReps",
DROP COLUMN "maleLoadAmountKgs",
DROP COLUMN "maleReps",
ADD COLUMN "distanceUnit" "DistanceUnit" NOT NULL DEFAULT E'METRES',
ADD COLUMN "loadAmountKgs" Decimal(65,30)   ,
ADD COLUMN "reps" integer   ,
ALTER COLUMN "repType" SET DEFAULT E'REPS';

ALTER TABLE "public"."WorldRecord" ALTER COLUMN "workoutScoreType" SET DEFAULT E'AMREPS';
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200615213450-workout-pyramid-structure-default..20200616164228-workout-move-remove-gendering
--- datamodel.dml
+++ datamodel.dml
@@ -6,9 +6,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 model Benchmark {
   id               String          @default(uuid()) @id
@@ -133,12 +133,11 @@
   id                  String             @default(uuid()) @id
   createdAt           DateTime           @default(now())
   sortPosition        Int
   repType             WorkoutMoveRepType @default(REPS)
-  maleReps            Int?
-  femaleReps          Int?
-  maleLoadAmountKgs   Float?
-  femaleLoadAmountKgs Float?
+  reps                Int?
+  loadAmountKgs       Float?
+  distanceUnit        DistanceUnit       @default(METRES)
   description         String?
   move                Move               @relation(fields: [moveId], references: [id])
   moveId              String
   selectedEquipment   Equipment?         @relation(fields: [selectedEquipmentId], references: [id])
@@ -170,9 +169,8 @@
   name              String
   summary           String?
   description       String?
   timecap           Int?
-  genderEquality    Boolean            @default(true)
   difficultyLevel   DifficultyLevel    @default(ONE)
   scope             AccessScopeType    @default(PRIVATE)
   workoutScoreType  WorkoutScoreType   @default(AMREPS)
   worldRecords      WorldRecord[]
@@ -324,4 +322,11 @@
 enum UnitSystem {
   IMPERIAL
   METRIC
 }
+
+enum DistanceUnit {
+  METRES
+  KILOMETRES
+  YARDS
+  MILES
+}
```



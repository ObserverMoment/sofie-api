# Migration `20200929120847-difficulty-level-now-integer`

This migration has been generated by ObserverMoment at 9/29/2020, 1:08:47 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."LoggedWorkout" DROP COLUMN "difficultyLevel",
ADD COLUMN "difficultyLevel" integer   NOT NULL DEFAULT 50

ALTER TABLE "public"."Workout" DROP COLUMN "difficultyLevel",
ADD COLUMN "difficultyLevel" integer   NOT NULL DEFAULT 50

DROP TYPE "DifficultyLevel"
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200928202009-remove-scheduled-workout-completed-on..20200929120847-difficulty-level-now-integer
--- datamodel.dml
+++ datamodel.dml
@@ -6,9 +6,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 // model ScoreSubmission {
 //   id                     String                @default(uuid()) @id
@@ -164,9 +164,9 @@
   imageUrl          String?
   timecap           Int?
   // structureData holds config info for certain workout types. E.g. Circuits and Tabatas.
   builderData       Json?
-  difficultyLevel   DifficultyLevel    @default(ONE)
+  difficultyLevel   Int                @default(50)
   scope             AccessScopeType    @default(PRIVATE)
   workoutType       WorkoutType        @relation(fields: [workoutTypeId], references: [id])
   workoutTypeId     String
   workoutSections   WorkoutSection[]
@@ -280,9 +280,9 @@
   videoUrl             String?
   videoThumbUrl        String?
   imageUrl             String?
   duration             Int?
-  difficultyLevel      DifficultyLevel   @default(ONE)
+  difficultyLevel      Int               @default(50)
   workoutType          WorkoutType       @relation(fields: [workoutTypeId], references: [id])
   workoutTypeId        String
   workoutSections      WorkoutSection[]
   originalWorkout      Workout?          @relation(fields: [originalWorkoutId], references: [id])
@@ -350,15 +350,8 @@
   FEMALE
   UNSPECIFIED
 }
-enum DifficultyLevel {
-  ONE
-  TWO
-  THREE
-  FOUR
-}
-
 enum WorkoutMoveRepType {
   REPS
   CALORIES
   DISTANCE
```



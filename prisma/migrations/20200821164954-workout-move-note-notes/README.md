# Migration `20200821164954-workout-move-note-notes`

This migration has been generated by ObserverMoment at 8/21/2020, 5:49:54 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."ScoreSubmission" DROP COLUMN "note",
ADD COLUMN "notes" text   ;

ALTER TABLE "public"."ScoreSubmissionVideo" DROP COLUMN "note",
ADD COLUMN "notes" text   ;

ALTER TABLE "public"."WorkoutMove" DROP COLUMN "note",
ADD COLUMN "notes" text   ;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200818105715-workout-original-access-scope-required..20200821164954-workout-move-note-notes
--- datamodel.dml
+++ datamodel.dml
@@ -6,16 +6,16 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model ScoreSubmission {
   id                     String                @default(uuid()) @id
   createdAt              DateTime              @default(now())
   completedOn            DateTime
-  note                   String?
+  notes                  String?
   completedBy            User                  @relation(fields: [completedById], references: [id])
   completedById          String
   challenge              Challenge?            @relation(fields: [challengeId], references: [id])
   challengeId            String?
@@ -26,9 +26,9 @@
 model ScoreSubmissionVideo {
   id                              String                           @default(uuid()) @id
   createdAt                       DateTime                         @default(now())
   videoUrl                        String
-  note                            String?
+  notes                           String?
   scoreSubmission                 ScoreSubmission
   scoreSubmissionId               String
   scoreSubmissionVideoValidations ScoreSubmissionVideoValidation[]
 }
@@ -249,9 +249,9 @@
   loadUnit            LoadUnit           @default(KG)
   // For logging. Number of seconds it took to complete the workoutMove
   duration            Int?
   description         String?
-  note                String?
+  notes               String?
   move                Move               @relation(fields: [moveId], references: [id])
   moveId              String
   selectedEquipment   Equipment?         @relation(fields: [selectedEquipmentId], references: [id])
   selectedEquipmentId String?
```


